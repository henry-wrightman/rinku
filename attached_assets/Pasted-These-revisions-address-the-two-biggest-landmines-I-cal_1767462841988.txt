These revisions address the two biggest landmines I called out last time (padding + deterministic reconstruction), and the multi-proof spec is now actually implementable instead of “conceptual.” Nice.

Here’s what looks great, what still conflicts, and the next fixes I’d do (some are repeats you still haven’t fully aligned).

What’s now really solid
1) Canonical padding is specified (no more “hash:'padding'”)

You replaced the placeholder with a deterministic EMPTY_NODE = { hash: SHA256("rinku:empty_node:v1"), sumWeight: 0 } and you use it consistently in the v5 reconstruction algorithm. 

WHITEPAPER (9)

 

WHITEPAPER (9)


That’s the right kind of explicitness.

2) Deterministic sparse-map reconstruction is spelled out

The layer-by-layer rule (“left MUST be present; right may be EMPTY_NODE”) is clear and eliminates verifier ambiguity. 

WHITEPAPER (9)

3) Profile C verification flow is now correct

It explicitly includes: bitmap→indices, multiproof reconstruction, root check (hash+totalWeight), pubkey aggregation, BLS verify, signerWeight ≥ 67% of derived totalWeight, chainId check, PoP requirement. 

WHITEPAPER (9)


This is the skeleton you want.

4) QR contradiction is resolved cleanly (without vs with multiproof)

You split the matrices into “without multiproof (v4)” and “with multiproof (v5),” and the N≤21 QR claim is now tied to packed+DEFLATE+v5. 

WHITEPAPER (9)

 

WHITEPAPER (9)

The big remaining inconsistency (important)
Transaction schema says uint64, but other sections still use number

You fixed the Tx structure in Section 2.1 to amount: uint64, fee: uint64, etc. 

WHITEPAPER (9)


But other places still show:

checkpoint header fields as height: number, timestamps as number 

WHITEPAPER (9)

account state uses balance: number, nonce: number 

WHITEPAPER (9)

smart contract call args as any[] (fine for app-level, but consensus encoding needs determinism) 

WHITEPAPER (9)

If you want “deterministic replay across validators,” you should enforce “integers only” and explicit widths everywhere consensus touches. You already say that in A.2. 

WHITEPAPER (9)


So I’d just align the rest of the schemas to match (even if you keep TypeScript types loose in code).

Concrete paper change: swap number → uint64/uint32 consistently in:

checkpoint header (height, timestamp) 

WHITEPAPER (9)

account model (balance, nonce, firstTxTimestamp) 

WHITEPAPER (9)

Two multiproof-spec improvements that will save you pain
1) Include committee ordering / index assignment rule explicitly

Your multiproof assumes the verifier knows that “bitmap_index” corresponds to a unique validator leaf at that index. 

WHITEPAPER (9)


But your consensus section says committee selection might be “Top N by stake” or rotating selection. 

WHITEPAPER (9)

That means: the committee list (and its ordering) must be unambiguous for a given checkpoint. Right now, Profile C reconstructs a root, but it doesn’t specify how validators are ordered into indices (0..N-1) in that checkpoint.

Minimal fix: add one sentence:

“Committee indices are assigned by sorting the selected committee deterministically by (stakeWeight desc, then ecdsa fingerprint asc) as of checkpoint h−1”
(or whatever you’re using).

Otherwise two nodes might build different trees from the same committee set, even if they agree on the members.

2) In reconstruction, require layerSize to be derived from committeeSize exactly as written

You do layerSize = ⌈N / 2^l⌉. 

WHITEPAPER (9)


That’s good. Just make sure your tree builder uses the same rule, not nextPow2 padding. With your EMPTY_NODE rule, you don’t need nextPow2, but you must have builder/verifier match.

I’d add one line in Appendix A.3:

“Tree depth is ⌈log₂N⌉ and layers use layerSize = ⌈N/2^l⌉; missing right children use EMPTY_NODE.”

Right now A.3 defines EMPTY_NODE but doesn’t define layerSize/depth rules as explicitly as Appendix F does. 

WHITEPAPER (9)

A few redundant prior callouts you still haven’t addressed
Tokenomics table still visually screams “math error”

Epoch 0 “31.5M” still distracts, even with the footnote. 

WHITEPAPER (9)


Since you said it’s placeholder, I’d just change the table to “Capped emission” and keep the theoretical in a footnote, or mark Section 6 as “draft.”

“Infrastructure dependency eliminated” is still slightly too strong in abstract/intro

Your abstract says “trustless verification without trusted infrastructure—data availability is provided by sender or network.” 

WHITEPAPER (9)


That’s mostly fine now, but I’d tweak “eliminate the need for trusted infrastructure” to “eliminate the need to trust infrastructure for verification.” (You already imply this in several spots; just make the headline match.)

Net: I think this is the best version yet

The multiproof + deterministic sparse-map + canonical EMPTY_NODE makes Profile C feel real. 

WHITEPAPER (9)


And the QR matrices now match the multiproof story. 

WHITEPAPER (9)

If you do 3 edits next:

Define committee ordering → index assignment rule (critical for tree determinism).

Align all remaining “number” schema fields to explicit uint widths (paper correctness).

Slightly reword “infrastructure dependency” language to “no trusted infrastructure required for verification.”