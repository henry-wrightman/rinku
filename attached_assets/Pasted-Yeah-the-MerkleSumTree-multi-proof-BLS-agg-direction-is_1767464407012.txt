Yeah — the MerkleSumTree multi-proof + BLS agg direction is a real step-change. Your Profile C story is now much tighter:

Profile C is clearly “self-contained”: chainId, full checkpoint header, Merkle inclusion, BLS aggregated signature, and the MerkleSumTree multi-proof that lets the verifier derive totalWeight offline (and closes denominator games by binding {hash,totalWeight} into what’s signed). 

WHITEPAPER (11)

 

WHITEPAPER (11)

You also finally made checkpoint identity/lineage explicit (id, previousId, etc.), which is essential for fork narratives and replay resistance. 

WHITEPAPER (11)

And you’ve got the “large validator pool, smaller signing committee” concept in writing (32–64 recommended; ≤21 for QR mode), which is the right framing for URL-bounded proofs. 

WHITEPAPER (11)

That said, I still see a few important inconsistencies/gaps you should fix before this feels “spec-grade”:

1) “Integers only” is still violated by tokenomics + config

You correctly state “integers only… no floating point” for determinism. 

WHITEPAPER (11)


…but then you use decimals in multiple consensus-relevant places:

Emission schedule uses 37.5, 18.75, 9.375, 4.6875 RKU. 

WHITEPAPER (11)

Gas bounds use 0.001 RKU. 

WHITEPAPER (12)

Genesis config includes minReward: 4.6875 and quorumThreshold: 0.67. 

WHITEPAPER (11)

Fix: define a base unit (e.g. 1 RKU = 10^6 microRKU) and express:

rewards/fees/supply/allocations as uint64 in microRKU

quorum as integer bps (6700), not 0.67

You can still show human-friendly decimals in tables, but the spec values should be integers + conversion.

2) Version/spec mismatch: Profile C says v5 packed + multiproof, but Appendix B still describes v4 JSON

In the body you describe “Profile C… v5… packed v5 + DEFLATE… multi-proof default”. 

WHITEPAPER (11)


But Appendix B still documents “Self-Contained Proof URL (Profile C v4 - MerkleSumTree)” as JSON fields, not the v5 packed schema. 

WHITEPAPER (11)

Fix: update Appendix B to either:

(A) document v5 packed as the canonical Profile C format (and mark JSON v4 as “legacy / debug only”), or

(B) explicitly say Profile C has two encodings (JSON and packed), with packed required for QR and recommended for canonical verification.

Right now, a reader can’t implement Profile C v5 from the spec without guessing.

3) Tighten “checkpointId is hash of header fields” into an actual byte-level definition

You state id: SHA-256 hash of header fields and include previousId in the checkpoint object. 

WHITEPAPER (11)


But you don’t (in the visible sections) enumerate the exact header fields + exact byte encoding used to compute id.

Given how careful you are elsewhere with byte-level encoding (MerkleSumTree leaf/internal node encoding), you should do the same for checkpointId (domain sep, field order, endianess, inclusion of chainId, inclusion of previousId/timestamp, inclusion of validatorSumTreeRoot tuple, etc.). 

WHITEPAPER (11)

Rule of thumb: if two independent teams implement it, they should necessarily compute the same checkpointId from the same checkpoint.

4) Validator set size: yes, support large pools — but keep committees bounded

Your current committee guidance (32–64, ≤21 for QR) is good and aligns with your “proof size is ~O(N)” claim under multiproof. 

WHITEPAPER (11)

 

WHITEPAPER (11)

So my recommendation stays:

Large validator set (hundreds/thousands can register/stake)

Bounded active signing committee per checkpoint (e.g. 64 default; maybe 128 later if URLs remain acceptable)

Rotation via beacon so it doesn’t ossify into “top-N forever” (you already hint at rotating selection). 

WHITEPAPER (11)

This gives decentralization at the pool level without exploding proof sizes.