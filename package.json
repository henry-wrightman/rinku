{
  "name": "rinku",
  "version": "0.1.0",
  "private": true,
  "description": "A URL-native distributed ledger with DAG-based consensus",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test -w @rinku/core",
    "test:core": "npm run test -w @rinku/core",
    "dev:explorer": "npm run dev -w @rinku/explorer",
    "validate": "npx tsx scripts/validate-testnet.ts",
    "stress-test": "npx tsx scripts/stress-test.ts",
    "mega-stress": "npx tsx scripts/mega-stress.ts",
    "activity-bot": "npx tsx scripts/activity-bot.ts",
    "fast-path-bot": "npx tsx scripts/fast-path-bot.ts",
    "p2p-test": "npx tsx scripts/p2p-sync-test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@noble/curves": "^2.0.1",
    "@noble/hashes": "^2.0.1",
    "@types/express-rate-limit": "^5.1.3",
    "express-rate-limit": "^8.2.1",
    "prom-client": "^15.1.3",
    "serve": "^14.2.5",
    "snarkjs": "^0.7.6",
    "tsx": "^4.21.0",
    "vitest": "^4.0.16"
  }
}
